import { ToAddressPipe } from './to-address.pipe';

describe('ToAddressPipe', () => {
  it('create an instance', () => {
    const pipe = new ToAddressPipe();
    expect(pipe).toBeTruthy();
  });
});
